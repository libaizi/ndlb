# 工作记忆 - 脑洞量表项目

## 2026-05-14

- 构建了「脑洞量表 Top Ten Online」在线版桌游 Web 应用
- 技术方案：单文件 HTML + PeerJS (WebRTC P2P)，无需自建服务器
- 文件路径：`F:\财经新闻\brain-hole-scale\index.html`
- 功能：创建/加入房间(4位房间码)、出题、秘密数字分配(点击查看/隐藏)、拖拽排序、揭晓判定动画、5轮游戏循环、独角兽/大便指示物追踪
- 网络架构：房主-客户端模式，房主维护权威游戏状态，PeerJS免费信令+P2P直传
- 李白偏好：简洁现代风格、手动输入题目、拖拽排序、点击查看数字
- Bug修复：客户端收到your_number时需同时更新GS.players中的player.number，渲染时用me?.number ?? GS.myNumber做fallback
- 新增：房主踢人功能（等待房间列表中每行显示"踢出"按钮，被踢者收到kicked消息后自动返回大厅）
- 新增：详细错误诊断弹窗（classifyError 8种错误分类，每种有标题/原因/解决方案，重试按钮）
- 改进：连接可靠性优化
  - 添加 STUN/TURN 服务器配置（Google STUN + Open Relay Project TURN + Metered Global TURN + Twilio STUN）
  - 连接超时从 10s/15s 增加到 20s/30s
  - 添加心跳检测（8s间隔 ping/pong，15s无响应标记断连）
  - 连接进度显示（3步：信令服务器→连接房主→建立P2P通道）
  - 自动重试机制（失败后最多自动重试5次，间隔递增）
  - 更新 TURN 服务器凭证为 staticauth.openrelay.metered.ca
  - **中继模式（核心改进）**：连接失败后自动切换 iceTransportPolicy:'relay' 强制走TURN服务器
  - 加入房间界面增加"🔄 中继模式"复选框，用户可手动勾选
  - 智能重试策略：前3次普通模式→第4次自动切中继模式→继续重试
  - 错误弹窗增加"🔄 中继模式重试"按钮（黄色），一键切换中继
  - ICE连接状态监控（logICE/monitorICEConnection），记录candidate类型/ICE状态变化
  - 连接中实时显示诊断日志，错误弹窗显示完整诊断日志
  - 修复 iceDigLog→iceDiagLog 拼写错误
  - ⚠️ 编码损坏事故：PowerShell Set-Content 破坏了 UTF-8 编码（138处中文乱码），已用 Write 工具完整重写修复
  - 教训：绝对不能用 PowerShell 的 Set-Content/Out-File 修改包含中文的文件，必须用 Write 工具
- 游戏流程改进：游戏不再自动结束，由房主手动控制
  - 移除 finishRound 中的自动 game-over 判定（原：独角兽达标或大便>=3自动结束）
  - 状态栏移除固定"/5"轮数显示，改为"第X轮"
  - round dots 改为动态 R{round} 标签
  - roundEnd 界面：房主看到"下一轮"+"结束游戏"两个按钮，客户端看到"等待房主操作"
  - 新增 doEndGame() 函数，房主点击后广播 game_over 给所有客户端
  - gameOver 界面改为显示"共X轮"的汇总，不再有胜负判定
- 出题者（captain）不再获得数字
  - goPhase('peek') 只给非出题者分配数字，出题者 number=null 且自动 peeked=true
  - 修复原代码 your_number 重复发送的 bug（之前在 peek block 和 isHost block 各发一次）
  - 现在只给非host且非captain（number!=null）的客户端发送 your_number
  - 客户端收到 phase_change 进入 peek 阶段时，若为出题者自动标记 number=null, peeked=true
  - renderPeek：出题者显示"你是出题者，无需查看数字"而非数字卡片
  - renderAnswer：出题者不显示数字卡片
  - doReveal：不再将 hostPlayer 追加到揭晓列表，只揭晓排序中的非出题者
